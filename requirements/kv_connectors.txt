lmcache == 0.4.5
# LMCache imports cupy.testing at runtime, which imports pytest. Keep these
# explicit so every Ray worker image has the same native LMCache environment.
cupy-cuda13x == 14.1.0
pytest == 9.0.3
nixl >= 1.1.0 # Required for disaggregated prefill
mooncake-transfer-engine >= 0.3.8
